# API Configuration
# For Docker deployment, use relative paths (nginx proxy handles routing)
# For local development, uncomment and use localhost URLs
# VITE_API_URL=http://localhost:8080/api/v1
VITE_API_URL=/api/v1

# WebSocket Configuration
# For Docker deployment, use relative paths (nginx proxy handles routing)
# For local development, uncomment and use localhost URLs
# VITE_WS_URL=http://localhost:8080/ws
VITE_WS_URL=/ws
